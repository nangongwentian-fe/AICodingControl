# AI Coding Control

[English](./README_EN.md) | 简体中文

AI Coding Control 是一个强大的 AI 编程辅助工具，帮助开发者管理和同步各种 AI 工具的配置文件。

## ✨ 功能特性

- 🔄 **规则同步**：管理和同步 AI 编程工具的 .cursorrules、.windsurfrules 等规则文件
- 🛠️ **MCP 同步**：同步和管理 MCP（Model Context Protocol）服务器配置
- 🎯 **技能同步**：管理和同步 AI 工具的自定义技能（Skills）
- 💬 **命令同步**：同步和管理 AI 工具的自定义命令（Commands）
- 🎨 **现代化界面**：基于 Ant Design 的简洁美观的用户界面
- ⚡ **快速启动**：使用 Vite 构建，开发体验极佳
- 🖥️ **跨平台**：基于 Electron，支持 Windows、macOS 和 Linux

## 🚀 技术栈

- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite
- **桌面应用**：Electron
- **UI 组件库**：Ant Design
- **代码编辑器**：Monaco Editor
- **路由管理**：React Router
- **样式方案**：Sass + TailwindCSS
- **包管理器**：pnpm

## 📦 安装

确保你的系统已安装以下环境：

- Node.js >= 18
- pnpm >= 8

克隆仓库并安装依赖：

```bash
# 克隆仓库
git clone https://github.com/nangongwentian-fe/AICodingControl.git

# 进入项目目录
cd AICodingControl

# 安装依赖
pnpm install
```

## 🛠️ 开发

### 启动开发服务器

```bash
# 同时启动 Vite 前端和 Electron 主进程
pnpm run dev

# 或者分别启动
pnpm run dev:react    # 仅启动前端开发服务器
pnpm run dev:electron # 仅启动 Electron 主进程
```

### 代码规范检查

```bash
# 运行 ESLint 检查
pnpm run lint

# 自动修复 ESLint 问题
pnpm run lint:fix
```

## 📦 构建

### 构建应用

```bash
# 构建前端并打包 Electron 应用
pnpm run build

# 或者分步构建
pnpm run build:react    # 仅构建前端
pnpm run build:electron # 仅打包 Electron 应用
```

构建产物：
- 前端构建产物：`dist/`
- Electron 打包产物：`dist-electron/`

### 预览构建产物

```bash
pnpm run preview
```

## 📁 项目结构

```
AICodingControl/
├── src/                    # 前端源码
│   ├── pages/             # 页面组件
│   │   ├── RuleSync/      # 规则同步页面
│   │   ├── McpSync/       # MCP 同步页面
│   │   ├── SkillsSync/    # 技能同步页面
│   │   └── CommandsSync/  # 命令同步页面
│   ├── components/        # 公共组件
│   ├── hooks/            # 自定义 Hooks
│   ├── router/           # 路由配置
│   ├── assets/           # 静态资源
│   ├── styles/           # 样式文件
│   ├── types/            # TypeScript 类型定义
│   ├── App.tsx           # 根组件
│   └── main.tsx          # 应用入口
├── electron/              # Electron 主进程代码
│   ├── main.cjs          # 主进程入口
│   └── preload.cjs       # 预加载脚本
├── build/                # 构建资源（图标等）
├── public/               # 公共静态资源
├── dist/                 # 前端构建产物
├── dist-electron/        # Electron 打包产物
└── package.json          # 项目配置
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

提交代码前请确保：
1. 代码通过 ESLint 检查（`pnpm run lint`）
2. 遵循项目的代码风格和命名约定
3. 提交信息遵循 `type: 描述` 格式（如 `feat: 添加新功能`）

## 📄 许可证

MIT License

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者！
